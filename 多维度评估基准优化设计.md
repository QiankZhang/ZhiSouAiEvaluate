# 多维度评估基准 · 自由度优化设计

> 状态：**P0 已实现**（2026-09-03）。后端 `site/backend/scoring.py`（新增）+ `engine.py` / `main.py` / `report.py`；前端 `site/frontend/src/pages.jsx` + `styles.css`。
> 关联：[prd.md](prd.md) · [技术方案.md](技术方案.md) · [人工评估中心设计.md](人工评估中心设计.md) · [API.md](API.md)
> 触发场景：「博文总结」评估需要 0/1/2/3 四档 + 每档独立标准 + 一票否决，现有基准无法表达。

## 实现与设计的差异（以代码为准）

| 设计 | 实现 | 原因 |
| --- | --- | --- |
| `_reason_for(score:int)` 按 1~5 阈值 | 改为按 0~1 得分率；旧签名传 1~5 时兼容折算 | 分制不再固定 |
| 报告总分分布按 `display_scale` 动态分桶 | 统一按**归一得分率**分 5 档（0-20%…80-100%），跨分制通用 | 桶数稳定，前端图表不用改 |
| 人工复核前端本地重算总分 | 前端只回传维度分，**后端 `_recompute_adjusted` 按基准聚合配置重算** | 消除前后端算法漂移（§9 风险点） |
| `weighted_score.py` 参数化 | 未动（P1）；引擎已不依赖该脚本，改走 `scoring.py` | 缩小改动面 |
| 报告 xlsx 增加档位列 | 未加（P1） | — |

单测：`site/backend/tests/test_scoring.py`（11 例）、`test_benchmark_validation.py`（6 例）；`test_engine.py` / `test_report.py` 全部沿用旧断言仍通过（weighted_raw 逐位还原）。前端 `npm run build` 通过；浏览器验证了创建/编辑/详情/校验拦截全链路。

---

## 0. 一句话定位

把「多维度评估基准」从**固定 5 维、写死 1~5 分、只有一句维度说明**，升级为
**用户自定义维度 + 每维独立分制 + 每档独立评分标准 + 可配置聚合方式**。

后端仍是纯标准库 FastAPI + SQLite 快照，前端仍是 React + hash 路由、无新增构建依赖，
裁判员仍只输出一个 JSON。存量基准与历史任务不受影响。

---

## 1. 现状与痛点

### 1.1 现在能做什么

创建「多维度机制」基准时，用户只能：

- 增删维度、改维度名、设权重（`site/frontend/src/pages.jsx` 维度编辑器只有 `name` + `weight` 两个输入框）
- 权重合计必须 = 100%

### 1.2 硬编码的假设（散落在四处）

| 位置 | 写死的假设 |
| --- | --- |
| `engine.py::_output_spec_block` / `_SYSTEM_SHELL` | 提示裁判员「维度评分为 1~5 的整数，禁止小数、禁止越界」 |
| `engine.py::_clamp_score` | `max(1, min(5, ...))` |
| `engine.py::_weighted_total` | `Σ(score × weight) / 100`（默认权重合计 100） |
| `engine.py::GSB_GOOD_THRESHOLD` | `0.18`，基于 1~5 量纲的差值 |
| `main.py::_dim_score`（模拟引擎） | `max(1, min(5, ...))` |
| `report.py` | `LOW_SCORE_THRESHOLD = 3.0`、`DIM_LOW_MARK = 2`、总分分布 `range(1, 6)` 写死 5 个桶 |
| `pages.jsx` 人工复核弹窗 | 分数输入 `min="1" max="5"` |
| `pages.jsx` 人工复核重算总分 | `Σ(score × weight) / 100` |
| `skills/multi-dimension-evaluation/scripts/weighted_score.py` | `SCORE_MIN, SCORE_MAX = 1, 5`，`acc / 100.0` |
| `main.py::DEFAULT_DIMENSIONS` | 5 维、每维一句 `criteria`，且该 `criteria` 在多维度机制下前端**根本不可编辑** |

### 1.3 三个缺口

1. **分制单一**：只能 1~5 整数。要 0~3、0~10、二值（通过/不通过）、枚举（G/S/B）都做不到。
2. **没有「每档标准」**：裁判员只拿到一句维度说明，不知道「给 2 分 vs 3 分」的界线，打分一致性差。
3. **聚合方式单一**：永远是加权求和。无法表达「任一维度 = 0 → 整体不可用」这类否决规则。

---

## 2. 目标与非目标

### 2.1 目标

让用户在创建 / 编辑基准时能定义：

- **维度**：名称、说明、权重（可选）
- **分制**：整数区间（`min`~`max`）或枚举集合，逐维独立
- **每档评分标准**：每个分值 → 一段「什么样算这一档」的描述（rubric anchor）
- **聚合方式**：加权归一 / 加权原样 / 阈值分档
- **一票否决**：某维度低于阈值 → 整体判为最低档（覆盖「博文总结」的拦截需求）

### 2.2 非目标（本期不做，见 §7 分期）

- 完整的「规则定级矩阵」（多条件表达式 → 档位），留 P2
- 小数分、置信区间
- 维度库 / 跨基准复用维度
- 多轮对话维度

### 2.3 设计原则

- **超集 + 读时兼容**：新维度结构是旧结构的超集；读取存量基准时用 shim 补默认值，不批量改库。
- **单一 JSON 契约**：裁判员输出结构不变（`dimensions[].score` + 整体 `reason`），只是 `score` 的取值域随维度声明变化。
- **聚合逻辑收敛到一处**：新建 `scoring.py`，`engine.py`、`main.py` 模拟引擎、前端重算共用同一套换算规则。

---

## 3. 数据模型

### 3.1 维度对象（`benchmark.config.dimensions[]`）

**现在：**

```json
{ "key": "relevance", "name": "相关性", "weight": 20, "criteria": "结果是否直接命中用户查询意图" }
```

**改后（超集）：**

```json
{
  "key": "fact_consistency",
  "name": "事实一致性/无幻觉度",
  "weight": 30,
  "description": "评估总结是否 100% 忠实于原博，无歪曲、断章取义或 AI 幻觉",
  "scale": {
    "type": "integer",
    "min": 0,
    "max": 3,
    "levels": [
      { "value": 3, "label": "无损还原", "criteria": "完美提炼核心观点与立论逻辑，数据/人物关系完全真实，零幻觉" },
      { "value": 2, "label": "基本准确", "criteria": "准确提炼主要观点，仅个别不影响结论的边缘细节微调" },
      { "value": 1, "label": "信息失真", "criteria": "态度/结论跑偏，或掺入原博没提过的数据（微弱幻觉）" },
      { "value": 0, "label": "指鹿为马", "criteria": "严重跑题、黑白颠倒或整体幻觉虚构" }
    ]
  },
  "veto_below": 1
}
```

枚举型：

```json
{
  "key": "verdict", "name": "整体判定", "weight": 0,
  "scale": {
    "type": "enum",
    "levels": [
      { "value": "G", "label": "更好", "criteria": "…", "score": 5 },
      { "value": "S", "label": "持平", "criteria": "…", "score": 3 },
      { "value": "B", "label": "更差", "criteria": "…", "score": 1 }
    ]
  }
}
```

字段说明：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `key` | 是 | 前端自动生成（`dim_1`…），可显示不可改；用于结果对齐 |
| `name` / `description` | `name` 必填 | `description` 是给裁判员的维度总述 |
| `weight` | 否 | 仅加权聚合模式用；留空或全 0 = 等权 |
| `scale.type` | 是 | `integer` \| `enum` |
| `scale.min` / `max` | integer 型必填 | 整数区间边界；允许 `min = 0` |
| `scale.levels[]` | 是 | 每档一项：`value`（整数或枚举串）、`label`（档位名）、`criteria`（该档标准）。integer 型允许只给部分档写 `criteria`，未写的档 UI 显示「未定义」 |
| `scale.levels[].score` | enum 型选填 | 枚举值 → 数值分，用于参与总分；不给则该维度不进总分、仅展示 |
| `veto_below` | 否 | 一票否决阈值：该维度得分 `< veto_below` → 整体判最低档（见 §4.3） |

### 3.2 基准级聚合配置（`benchmark.config.scoring`）

```json
{
  "mode": "weighted_normalized",
  "display_scale": 5,
  "low_score_ratio": 0.5,
  "grade_thresholds": [
    { "min_ratio": 0.90, "label": "3档 · 极致提效" },
    { "min_ratio": 0.75, "label": "2档 · 安全无错" },
    { "min_ratio": 0.0,  "label": "1档 · 不可用" }
  ]
}
```

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `mode` | `weighted_raw` | `weighted_raw`（旧算法：`Σ(score×weight)/100`，仅当全维度同分制时可选）／ `weighted_normalized`（每维归一到 0~1 再加权，混合分制唯一选择）／ `threshold`（在归一分基础上套 `grade_thresholds` 分档） |
| `display_scale` | `5` | 归一分对外展示时的满分：`5` / `100` / `grade`（只显示档位名） |
| `low_score_ratio` | `0.6` | 得分率低于此值的样本 / 维度记为「低分」，替代写死的 `3.0` / `2` |
| `grade_thresholds[]` | 无 | `mode = threshold` 时的分档表，按 `min_ratio` 从高到低匹配 |

**归一化公式**：`ratio_i = (score_i - min_i) / (max_i - min_i)`；
`total_ratio = Σ(ratio_i × weight_i) / Σ(weight_i)`；
展示分 = `total_ratio × display_scale`（`display_scale = grade` 时输出档位名）。

### 3.3 存量兼容（读时 shim）

`scoring.py::normalize_benchmark(config)`：

- 维度无 `scale` → 补 `{ type: "integer", min: 1, max: 5, levels: [用 criteria 生成一条 value=5 的档] }`
- `config` 无 `scoring` → 补 `{ mode: "weighted_raw", display_scale: 5, low_score_ratio: 0.6(旧语义:3.0/5) }`
- `weighted_raw` 模式下，`_weighted_total` 完全走旧公式 `Σ(score×weight)/100`，**逐位还原当前行为**（回归测试保证）

存量基准记录不改写；下次用户编辑保存时才落新结构。历史任务结果的 `scores` 结构本就没变，照常渲染。

---

## 4. 打分与聚合流程

### 4.1 裁判员提示词组装（`engine.py`）

| 现在 | 改后 |
| --- | --- |
| `_dimensions_block`：`- key/name（权重 w）：criteria` | 每维追加分制声明与每档标准：<br>`- fact_consistency / 事实一致性（权重 30，取值 0~3）`<br>`  · 3 无损还原：…`<br>`  · 2 基本准确：…` |
| `_output_spec_block`：`"score": <1-5 整数>` | 按维度生成：整数型 `<0~3 整数>`，枚举型 `<"G"｜"S"｜"B">` |
| `_SYSTEM_SHELL` 规则 3：「1~5 的整数」 | 「严格按每个维度声明的取值范围 / 枚举集合打分，不得越界」 |

### 4.2 分值校验（`engine.py::_clamp_score` → `scoring.py::coerce_score(dim, raw)`）

- 整数型：四舍五入后 clamp 到 `[min, max]`
- 枚举型：必须命中某个 `level.value`，否则计为 schema 校验失败（触发现有的 ≤2 次轻量重试）

### 4.3 一票否决

聚合前先扫一遍 `veto_below`：

- 任一维度 `score < veto_below` → `total_ratio = 0`，档位 = 最低档，`reason` 前缀标注「[否决] {维度名} 触发拦截」
- 「博文总结」的映射：`fact_consistency.veto_below = 2`（信息失真/幻觉即拦截）、其余三维 `veto_below = 1`（出现 0 分即拦截）

> 说明：这解决了「0 档拦截」这类**安全底线**规则。「1 档 vs 2 档 vs 3 档」这种正向分级本期用 `threshold` 近似（加权得分率分档）；要 100% 复刻「去冗与扫读同为满分才 3 档」的与/或矩阵，需 P2 的规则引擎。

### 4.4 GSB 机制

- `GSB_GOOD_THRESHOLD` 从「1~5 量纲的 0.18」改为「归一化 0~1 量纲的可配置值」（默认 `0.05`，等价于旧的 0.18/满分区间），放进 `config.scoring.gsb_good_threshold`
- 每维仍分别给实验 / 基线打分，按各维 `scale` 校验

### 4.5 模拟引擎（`main.py::_evaluate_item`）

无 Key 降级时：`_dim_score` 按维度 `scale` 生成落在 `[min, max]` 内的确定性分；聚合调用同一个 `scoring.py`。保证有 / 无 Key 两条路径量纲一致。

---

## 5. 报告与下游

| 模块 | 改动 |
| --- | --- |
| `report.py` 维度统计 | `avg` 改为同时给「均分 / 满分」与「得分率」；混合分制的整体排序用得分率 |
| `report.py` 低分判定 | `LOW_SCORE_THRESHOLD` / `DIM_LOW_MARK` → `config.scoring.low_score_ratio`（得分率） |
| `report.py` 总分分布 | `range(1, 6)` → 按 `display_scale` 动态分桶；`mode = threshold` 时按档位名分布 |
| `report.py` Markdown / xlsx | 维度列增加「档位」列（有 `levels.label` 时）；总分列注明量纲（如「/5」「/100」「档位」） |
| `pages.jsx::ResultScores` / 明细表 | 维度分展示 `x / max`；枚举维度显示 `label`；分布图按动态桶 |
| `pages.jsx` 人工复核弹窗 | 分数输入按该维 `scale` 渲染：整数 → `min/max` 数字框 + 每档 anchor 悬浮提示；枚举 → 下拉 |
| `pages.jsx` 人工复核重算总分 | 前端引入与后端一致的 `scoring` 换算（或改为提交后由后端回算并返回） |
| `weighted_score.py`（skill 脚本） | 增加 `--mode normalized` 与逐维 `min/max` 入参；旧调用保持默认行为 |
| `multi-dimension-evaluation/SKILL.md` | 正文去掉「固定五维、1~5」的硬表述，改为「维度、分制、每档标准以基准配置为准」 |
| `main.py::DEFAULT_DIMENSIONS` / 人工评估中心默认维度 | 每维补 `scale`（默认 1~5 五档，带 anchor 文案） |
| `API.md` / `validate-prompt` | dimensions 增加 `scale` 结构校验；dry-run 预览按新结构渲染 |
| `db.py` | 无需改（整份 JSON 快照）；仅在 `normalize_benchmark` 处保证读回时补齐 |

---

## 6. 前端交互设计

### 6.1 维度编辑器：从「表格行」升级为「维度卡片」

```
┌─ 维度 1 ───────────────────────────────  [权重 30 %]  [🗑] ─┐
│ 维度名称  [事实一致性/无幻觉度                              ] │
│ 维度说明  [评估总结是否 100% 忠实于原博…                    ] │
│ 分制      (●) 整数区间  min [0]  max [3]     ( ) 枚举        │
│ 一票否决  得分 <  [2]  时整体判最低档   (可关闭)             │
│ ── 各档评分标准 ────────────────────────────────────────── │
│   3  档位名[无损还原]  标准[完美提炼…零幻觉                ] │
│   2  档位名[基本准确]  标准[准确提炼主要观点…              ] │
│   1  档位名[信息失真]  标准[态度/结论跑偏…                 ] │
│   0  档位名[指鹿为马]  标准[严重跑题、黑白颠倒…            ] │
└────────────────────────────────────────────────────────────┘
[+ 添加维度]   [套用模板 ▾]
```

- **套用模板 ▾**：`五档制(1~5)` / `四档制(0~3，博文总结)` / `十分制(0~10)` / `GSB(G/S/B)` / `二值(通过/不通过)` — 一键把当前维度的 `scale` 填成模板，`criteria` 留空待填。
- 改 `min/max` 时自动增删档位行，已填的 `criteria` 尽量保留。
- 枚举模式：档位行变为「标签 + 数值分（选填）+ 标准」。

### 6.2 基准级设置区（维度列表下方）

- **聚合方式**：`加权求和（同分制）` / `加权归一（混合分制）` / `阈值分档`
  - 选「加权求和」但检测到维度分制不一致 → 禁用并提示「维度分制不同，请用加权归一」
  - 选「阈值分档」→ 展开 `grade_thresholds` 编辑器（得分率 → 档位名）
- **展示满分**：`5 分` / `100 分` / `只显示档位`
- **低分阈值**：得分率 `[60] %`
- **权重合计**校验：仅「加权求和 / 加权归一」模式生效；等权时不校验

### 6.3 校验规则

| 规则 | 触发 |
| --- | --- |
| 每个维度至少 1 个档位 | 保存拦截 |
| 档位 `value` 不重复、整数型覆盖 `min~max` | 保存拦截 |
| 加权模式下权重合计 = 100%（或全 0 = 等权） | 保存拦截 |
| `veto_below` 在 `[min+1, max]` 区间内 | 保存拦截 |
| 有档位缺 `criteria` | 黄色提醒，可保存（裁判员按档位名兜底） |

---

## 7. 分期

### P0 — MVP（本次）

- 维度自定义分制：整数区间 + 枚举
- 每档评分标准（rubric anchor）
- 聚合：`weighted_raw` + `weighted_normalized`
- 一票否决 `veto_below`
- `scoring.py` 抽取；`engine.py` / 模拟引擎 / `report.py` / 前端适配
- 读时 shim + 存量兼容回归测试
- 「套用模板」下拉（含「四档制·博文总结」）

### P1

- `threshold` 阈值分档 + `display_scale` 可选
- 报告 / 导出的档位列与量纲标注
- 人工复核弹窗按 `scale` 动态渲染
- `weighted_score.py` 脚本参数化

### P2

- **规则定级矩阵** `grade_matrix`：多条「条件表达式 → 档位」规则，按序匹配，命中即停
  （完整复刻「博文总结」：`concision=3 且 scannability=3 且 四维≥2 → 3档`）
- 维度库（跨基准复用）
- 多轮对话维度

---

## 8. 开放问题（待产品方拍板，先给默认值）

| # | 问题 | 默认结论 |
| --- | --- | --- |
| 1 | 维度可不设权重（等权）吗？ | **可以**。权重留空 / 全 0 = 等权归一 |
| 2 | 混合分制时总分怎么展示？ | 统一按「得分率」，`display_scale` 决定折算成 5 / 100 / 档位 |
| 3 | 枚举维度是否进总分？ | 给了 `level.score` 才进；否则仅展示 |
| 4 | 存量基准何时迁移到新结构？ | 不主动迁移，用户下次编辑保存时落新结构；读取始终走 shim |
| 5 | 本期是否做规则定级矩阵？ | **否**。P0 用「一票否决 + 阈值分档」覆盖 80% 场景，完整矩阵留 P2 |
| 6 | 旧任务用旧基准重跑还能对齐吗？ | 能。`weighted_raw` 模式逐位还原旧公式，回归测试锁定 |
| 7 | 「博文总结」这类要不要直接内置成模板基准？ | 作为「套用模板」里的一个 `scale` 预设，不预置整条基准记录 |

---

## 9. 影响面清单（供评审快速过）

**后端**：`engine.py`（提示词组装 / 校验 / 阈值）、`main.py`（模拟引擎 / DEFAULT_DIMENSIONS / benchmark body）、`report.py`（统计 / 分布 / 导出）、新增 `scoring.py`、`skills_registry` 无改动、`db.py` 无改动、`manual.py`（默认维度带 scale）
**前端**：`pages.jsx`（维度卡片编辑器 / 基准设置区 / 校验 / ResultScores / 分布图 / 人工复核弹窗 / 重算总分）、`api.js`（常量对齐）
**Skill**：`multi-dimension-evaluation/SKILL.md`、`scripts/weighted_score.py`
**文档**：`API.md`、`技术方案.md` 评估基准章节

**风险点**：
- `report.py` 总分分布与前端图表对 1~5 的隐式依赖较多，需逐处排查
- 人工复核「前端本地重算总分」与后端 `scoring.py` 必须同源，否则复核前后数字对不上
- GSB 阈值量纲切换需要重算一遍历史 GSB 任务的判定不变（回归）
