# 组织与账号登录系统 · 产品设计

> 状态：**已实现**（2026-08-31）。下文为原始设计稿，实现时按「最小改动、不过度实现」做了几处收敛，见本节。
> 关联：[prd.md](prd.md) · [技术方案.md](技术方案.md) · 实现 `site/backend/accounts.py`、`site/frontend/src/account.jsx`

## 0. 实现与设计的差异（以代码为准）

| 设计原文 | 实际实现 | 原因 |
| --- | --- | --- |
| 业务对象加 `org_id` 字段，启动回填，列表按 `org_id` 过滤 | **不加 `org_id`**。只有一个组织，可见性等价于「当前账号是否在组织内」，由中间件统一判定：在组织内→放行全部业务接口，孤立账号→`403` | 单组织下逐条 `org_id` 恒等式过滤是纯冗余；退出组织即 403，效果一致 |
| 每个业务路由挂 `Depends(require_org_member)` | 一个 `@app.middleware("http")` 集中鉴权，业务路由零改动 | 少碰 ~40 个 handler |
| 拆 `auth.py` + `org.py` 两个模块 | 合并为单个 `accounts.py`（约 300 行） | 文件更少 |
| CSRF 用 `SameSite=Lax` + `X-Requested-With` 头双保险 | 只用 `SameSite=Lax` Cookie | 内网小工具，Lax 已挡住跨站表单提交；少一处前后端约定 |
| `organizations` 建表持久化 | 默认组织是模块常量 `DEFAULT_ORG`，不入库 | 本期不支持建/改组织 |
| 历史数据 `created_by` 迁移 = 改字符串 + 绑定账号 | 仅把字符串 `"孙颖"` → `"张乾坤"`（`created_by` 本就是展示字符串，无独立归属字段） | 无需引入新字段 |

新增测试：`site/backend/tests/test_accounts.py`（密码哈希、种子幂等、迁移、加成员/邀请/退出）。前端 `npm run build` 通过。

---

## 1. 背景与目标

当前「智搜策略效果评估」平台**没有任何登录与鉴权**：顶部导航写死「孙颖」，所有数据集 / 评估基准 / 评估任务都是全局可见，`created_by` 只是一个展示字符串。

本次要引入一个**轻量的「组织—账号」体系**，达成：

1. 用账号密码登录，登录后知道「我在哪个组织」。
2. 预置一批默认账号，统一加入默认组织「**智搜产品**」。
3. 组织内成员可以互相看到组织下的**全部**数据集、评估基准、评估任务及其它业务数据。
4. 支持修改密码。
5. 顶部导航栏有明显的组织标识；支持用户**退出组织**变成「孤立账号」。
6. 组织内任意成员都能**新增用户**（输入姓名 + 账号即可），也能把已有的孤立账号**邀请进组织**。

**设计原则**：与现有工程一致 —— 后端纯标准库 FastAPI + SQLite 快照（见 `site/backend/db.py`），前端 React + hash 路由、无新增构建依赖；不引入 RBAC 框架、不引入 OAuth/SSO、不引入邮件。

---

## 2. 需求确认（已与产品方对齐）

| # | 问题 | 结论 |
| --- | --- | --- |
| 1 | 退出组织、成为孤立账号后能看到哪些业务数据？ | **看不到任何业务数据**（数据集 / 基准 / 任务全部不可见），只能登录、改密、等待被邀请 |
| 2 | 孤立账号如何（重新）加入「智搜产品」？ | **由组织内任意成员邀请 / 拉入**，没有自助加入、没有审批流 |
| 3 | 账号如何创建？是否开放自助注册？ | **仅组织成员新增，无自助注册**；登录页只有「登录」，没有「注册」 |
| 4 | 组织成员「新增用户」时密码怎么定？ | **统一初始密码 `12345678`**，创建表单只填姓名 + 账号 |

---

## 3. 概念模型

```
组织 Organization (1) ──────< 成员关系（account.org_id） >────── (0..1) 账号 Account
                                                                      │
                                                                      │ (1) ──< 会话 Session (n)
                                                                      │
业务对象 Dataset / Benchmark / Task  ── org_id ──> 组织
```

- **组织 Organization**：本期只有一个默认组织「智搜产品」（`ORG-1`）。数据结构按「多组织」预留，但 UI 不提供「新建组织」入口。
- **账号 Account**：`org_id` 可为空。
  - `org_id = ORG-1` → 组织成员，能看到组织下全部业务数据。
  - `org_id = null` → **孤立账号**，看不到任何业务数据，也不能创建业务数据。
  - 一个账号**最多属于一个组织**（不做多组织）。
- **会话 Session**：登录后服务端签发的不透明 token，写入 HttpOnly Cookie。
- **业务对象**：数据集 / 评估基准 / 评估任务新增 `org_id` 字段，创建时归属到「创建人当前所在组织」。

---

## 4. 默认组织与种子账号

### 4.1 默认组织

| id | 名称 | 说明 |
| --- | --- | --- |
| `ORG-1` | 智搜产品 | 首次启动时自动创建，不可删除、不可改名（本期） |

### 4.2 种子账号（首次启动自动写入，账号 = 姓名小写拼音）

| 姓名 | 账号 | 初始密码 | 组织 |
| --- | --- | --- | --- |
| 王烁 | `wangshuo` | `12345678` | 智搜产品 |
| 王婧雅 | `wangjingya` | `12345678` | 智搜产品 |
| 杨声璐 | `yangshenglu` | `12345678` | 智搜产品 |
| 荆欣芮 | `jingxinrui` | `12345678` | 智搜产品 |
| 高帅 | `gaoshuai` | `12345678` | 智搜产品 |
| 黄巍 | `huangwei` | `12345678` | 智搜产品 |
| 韩旭 | `hanxu` | `12345678` | 智搜产品 |
| 陈斯睿 | `chensirui` | `12345678` | 智搜产品 |
| 赵云鹏 | `zhaoyunpeng` | `12345678` | 智搜产品 |
| 刘思怡 | `liusiyi` | `12345678` | 智搜产品 |
| 张乾坤 | `zhangqiankun` | `12345678` | 智搜产品 |

**幂等**：种子逻辑只在「`accounts` 表为空」时执行一次；已存在账号时跳过，不会覆盖用户改过的密码。

**不新建 `sunying` 账号**：现有业务数据 `created_by` 全是「孙颖」，迁移时统一改归属到 `zhangqiankun`（张乾坤），见 §9。

---

## 5. 权限与可见性规则（核心）

本期是**扁平模型**：组织内成员**权限完全相同**，不区分管理员 / 普通成员。

| 能力 | 组织成员 | 孤立账号 | 未登录 |
| --- | --- | --- | --- |
| 登录 / 登出 | ✅ | ✅ | — |
| 修改自己的密码 | ✅ | ✅ | ❌ |
| 查看组织下数据集 / 基准 / 任务 / 概览 | ✅ 全部 | ❌ 空列表 | ❌ 401 |
| 创建 / 编辑 / 删除数据集 / 基准 / 任务 | ✅ | ❌（无归属组织） | ❌ |
| 执行 / 停止评测、人工复核、导出报告 | ✅ | ❌ | ❌ |
| 新增用户 / 邀请孤立账号入组织 | ✅ | ❌ | ❌ |
| 重置其他成员密码为 `12345678` | ✅ | ❌ | ❌ |
| 退出组织 | ✅ | —（已不在组织） | ❌ |

**可见性实现**：所有列表 / 详情接口在入口做一次归属校验 —— `require_org_member()`：

- 无有效会话 → `401 UNAUTHENTICATED`。
- 会话有效但 `account.org_id is None` → 列表接口返回空集合、写接口返回 `403 NO_ORGANIZATION`（前端渲染「你还没有加入任何组织」空状态）。
- 会话有效且是组织成员 → 过滤 `item.org_id == account.org_id`。

> 本期只有一个组织，`org_id` 过滤实际恒真；引入它是为了「退出组织即失去可见性」这条规则能干净落地，也为将来多组织留口子。

> 成员可**重置别人的密码**（重置为 `12345678`）：本期没有邮箱找回，人人可新增用户，故对称地开放重置。成员列表每行提供「重置密码」，重置后目标账号 `pwd_changed_at` 归零、其所有会话失效。

---

## 6. 认证方案

### 6.1 密码存储

- 纯标准库：`hashlib.pbkdf2_hmac("sha256", password, salt, 200_000)`。
- 存储格式：`pbkdf2_sha256$200000$<salt_hex>$<hash_hex>`，随算法可演进。
- **禁止**明文或可逆存储；日志中**禁止**打印密码 / 会话 token（对齐 CLAUDE.md §7）。
- 密码规则：长度 8–64，仅此一条（不强制大小写 / 符号，避免默认密码 `12345678` 反而不合规）。

### 6.2 会话

- 登录成功 → 生成 `token = secrets.token_urlsafe(32)`，写入 `sessions`，返回 `Set-Cookie: zs_session=<token>; HttpOnly; SameSite=Lax; Path=/`（生产走 HTTPS 时加 `Secure`）。
- 有效期：**14 天**滑动过期（每次请求刷新 `last_seen`，超过 14 天未活动失效）。
- 登出 → 删除该 token。
- 会话随 `db.py` 快照一起持久化（进程重启不掉线）。
- 前端 `api.js` 的 `fetch` 是**同源请求**，Cookie 自动携带，改动极小；只需新增「收到 401 → 跳登录页」的统一处理。

### 6.3 攻击面（够用即可）

- 登录接口按 `账号` + IP 简单限流：连续 5 次失败锁定 1 分钟（内存计数即可）。
- CSRF：写接口用 `SameSite=Lax` Cookie + 自定义头 `X-Requested-With` 校验（前端 `api.js` 统一加）。
- 不做验证码、不做 2FA、不做设备管理。

---

## 7. 数据模型（SQLite 快照新增）

沿用 `db.py` 的「整份状态 JSON 快照」方式，`load_state()` / `save_state()` 各加三个 key：`organizations` / `accounts` / `sessions`；`_id_seq` 增加 `ORG` / `ACC` 前缀。

```jsonc
// organizations[]
{ "id": "ORG-1", "name": "智搜产品", "created_at": "2026-08-31 10:00" }

// accounts[]
{
  "id": "ACC-1001",
  "account": "wangshuo",          // 登录名，全局唯一，创建后不可改
  "name": "王烁",                  // 展示名，可改
  "password_hash": "pbkdf2_sha256$200000$...$...",
  "org_id": "ORG-1",              // null = 孤立账号
  "status": "ACTIVE",             // 预留：DISABLED
  "created_at": "2026-08-31 10:00",
  "created_by": "系统",           // "系统" | 某个 account 的展示名
  "pwd_changed_at": null          // null 表示还在用初始密码（前端提示「建议修改密码」）
}

// sessions[]
{ "token": "…", "account_id": "ACC-1001", "created_at": "…", "last_seen": "…" }
```

**业务对象改动**：`_datasets` / `_benchmarks` / `_tasks` 的每个元素新增 `"org_id": "ORG-1"`。`created_by` 保留为展示字符串（值改为创建人的展示名）。

---

## 8. 关键流程

### 8.1 首次启动初始化

```
进程启动 → db.load_state()
  ├─ organizations 为空 → 写入 ORG-1「智搜产品」
  ├─ accounts 为空     → 写入 §4.2 的 11 个种子账号
  ├─ 业务对象缺 org_id → 全部回填为 ORG-1（见 §9）
  └─ created_by = "孙颖" → 改写为 "张乾坤"，归属人绑定 zhangqiankun（见 §9）
```

### 8.2 登录

```
POST /api/auth/login { account, password }
  ├─ 账号不存在 / 密码错 → 401 INVALID_CREDENTIALS（文案统一「账号或密码错误」，不区分）
  ├─ status = DISABLED → 403 ACCOUNT_DISABLED
  └─ 成功 → 签发会话，Set-Cookie，返回 { account, name, org: {id,name} | null }
```

前端登录页固定提示语：

> 默认账号为**姓名的小写拼音**，初始密码 **`12345678`**。登录后请尽快在「修改密码」中更新。

### 8.3 修改密码

```
POST /api/auth/change-password { old_password, new_password }
  ├─ old 校验失败 → 400 WRONG_OLD_PASSWORD
  ├─ new 不满足 8–64 → 400 WEAK_PASSWORD
  └─ 成功 → 更新 hash + pwd_changed_at，注销**其它**会话（当前会话保留）
```

### 8.4 新增用户（组织成员操作）

```
POST /api/org/members { name, account }
  ├─ 未登录 / 非组织成员 → 401 / 403
  ├─ account 已存在：
  │     ├─ 该账号是孤立账号 → 直接并入本组织（等价「邀请」），返回 joined:true
  │     └─ 该账号已在某组织 → 409 ACCOUNT_TAKEN
  ├─ account 不合法（非 ^[a-z0-9_]{2,32}$）→ 400
  └─ 新建：password_hash = hash("12345678")，org_id = 当前组织，created_by = 操作人
```

> 「新增用户」和「邀请孤立账号」用**同一个入口**：填账号，存在且孤立就拉进来，不存在就按姓名新建。UI 上文案写「添加成员」。

### 8.5 退出组织

```
POST /api/org/leave
  └─ account.org_id = null；该账号创建的业务数据**仍归属原组织**（不带走、不删除）
     退出后立即刷新前端：业务菜单进入空状态，顶部标识变为「未加入组织」
```

- 允许「最后一个成员」退出（组织可以变成 0 人，数据仍在，等有人被拉入后又可见）。本期不做「组织不能没人」的约束。

### 8.6 邀请 / 移出

- 邀请：见 §8.4（填对方账号）。
- 移出他人：`DELETE /api/org/members/{account_id}` → 把对方 `org_id` 置空（对方变孤立账号，其会话保留但业务数据即刻不可见）。带二次确认弹窗；不能移出自己（自己走「退出组织」）。

---

## 9. 迁移与兼容

| 项 | 处理 |
| --- | --- |
| 现有 `_datasets/_benchmarks/_tasks` 无 `org_id` | 启动时一次性回填 `org_id = "ORG-1"` |
| 现有 `created_by = "孙颖"` | 启动迁移时统一改写为 `张乾坤`，并把这些业务对象归属人绑定到账号 `zhangqiankun`；不再新建 `sunying` 账号 |
| 前端写死的「孙颖」 (`main.jsx:106`) | 换成当前登录账号的展示名 + 账号下拉菜单 |
| 已上线数据 `app.db` | 兼容：`load_state()` 读到旧快照 → 补默认组织/种子/回填后再 `save_state()` |
| 回滚 | 删除 `backend/data/app.db*` 三个文件即可回到全新状态；或保留旧代码分支 |

---

## 10. API 设计

统一前缀沿用现状 `/api`（不改成 `/api/v1`）；错误格式沿用现有 `{ "detail": ... }`。新增：

| 模块 | 方法 + 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| 认证 | `POST /api/auth/login` | 登录，签发会话 | 公开 |
| | `POST /api/auth/logout` | 登出当前会话 | 登录 |
| | `GET  /api/auth/me` | 当前账号 + 组织信息（前端启动时拉一次） | 登录 |
| | `POST /api/auth/change-password` | 改密码 | 登录 |
| 组织 | `GET  /api/org` | 当前组织信息 | 组织成员 |
| | `GET  /api/org/members` | 成员列表（姓名/账号/加入时间/添加人） | 组织成员 |
| | `POST /api/org/members` | 添加成员（新建 或 邀请孤立账号） | 组织成员 |
| | `DELETE /api/org/members/{account_id}` | 移出成员（二次确认，不能移出自己） | 组织成员 |
| | `POST /api/org/members/{account_id}/reset-password` | 重置为 `12345678` | 组织成员 |
| | `POST /api/org/leave` | 退出组织，变孤立账号 | 组织成员 |

**现有接口改动**：`/api/overview/*`、`/api/datasets*`、`/api/benchmarks*`、`/api/tasks*`、`/api/models` 全部加 `require_org_member` 依赖；列表按 `org_id` 过滤；创建时写入 `org_id` 和 `created_by = 当前账号展示名`。`/health` 保持公开。

---

## 11. 前端改动

| 位置 | 改动 |
| --- | --- |
| **登录页**（新增） | 账号 + 密码两个输入框；固定提示语（§8.2）；无「注册」「忘记密码」链接（改密需登录后操作，忘记找组织成员重置）。未登录访问任何页面 → 重定向到登录页 |
| **顶部导航** `main.jsx` topbar | ① 组织标识 chip：`🏢 智搜产品`（醒目底色）；孤立账号显示 `⚠️ 未加入组织`。② 右侧「孙颖」写死项 → 账号菜单：展示名 + 账号，下拉含「修改密码」「成员管理」（组织成员可见）「退出组织」「退出登录」 |
| **侧边导航** | 孤立账号：数据概览/评估中心/数据集/评估基准 显示为禁用或空状态引导「等待组织成员邀请你加入」 |
| **修改密码弹窗**（新增） | 旧密码 / 新密码 / 确认新密码；成功后提示「其它设备需重新登录」 |
| **成员管理页**（新增，`/members`） | 成员表格 + 「添加成员」按钮（姓名 + 账号，账号存在且孤立时提示「将邀请该账号加入」）；每行「重置密码」「移出组织」（二次确认） |
| `api.js` | 统一处理 `401 → location.hash = '/login'`；写请求加 `X-Requested-With` 头 |
| 「创建人」筛选项 | 数据源从「字符串去重」改为「组织成员列表」 |

**视觉**：组织 chip 用 `Design.MD` 里已有的品牌色 / Badge 组件规范，放在 `topbar-title` 右侧，保证「一眼可见当前组织」。

---

## 12. 非目标（本期不做）

- 多组织切换、组织管理后台、组织新建 / 改名 / 删除。
- 角色与细粒度权限（管理员 / 只读 / 复核员等 RBAC）。
- 邮箱、手机号、SSO / OAuth、企业微信登录、2FA。
- 自助注册、邮件找回密码、邀请链接。
- 操作审计日志（仅保留 `created_by` / `created_at`）。
- 数据在成员间的「私有 / 共享」区分 —— 组织内一律全可见。
- 按成员统计的数据权限报表。

---

## 13. 决策记录（已全部确认）

| 编号 | 事项 | 结论 |
| --- | --- | --- |
| A | 历史数据 `created_by = "孙颖"` 的归属 | **改归属到 `zhangqiankun`（张乾坤）**，不新建 `sunying` 账号 |
| B | 成员能否重置其他成员密码为 `12345678` | **能**（无邮箱，需找回途径），成员列表每行提供「重置密码」 |
| C | 成员能否把其他成员「移出组织」 | **能**，带二次确认，不能移出自己（自己走「退出组织」） |
| D | 会话有效期 | **14 天**滑动过期（内网 / 小团队工具） |
| E | 账号命名规则 `^[a-z0-9_]{2,32}$`，拼音重名 | 冲突时创建方自行加后缀（如 `wangshuo2`），系统只报「账号已存在」 |
| F | 孤立账号是否允许创建「不归属任何组织」的业务数据 | **不允许**（与「看不到任何业务数据」一致，避免产生游离数据） |

| 风险 | 应对 |
| --- | --- |
| 初始密码 `12345678` 全员相同、长期不改 | 登录页强提示 + 顶部对 `pwd_changed_at=null` 的账号显示「建议修改密码」气泡 |
| 仓库 / 部署为内网，无 HTTPS 时 Cookie 可被嗅探 | 生产环境 `nginx.conf` 增加 HTTPS 后开启 `Secure`；文档注明 |
| 快照存储会话，`app.db` 泄露即会话泄露 | token 仅 14 天有效；`app.db` 已在 `.gitignore` |
| 全组织可见 + 人人可删 → 误删风险 | 沿用现有「被引用不可删」约束；删除操作二次确认 |

---

## 14. 落地里程碑（供排期参考，本次不实施）

1. **后端骨架**：`auth.py`（密码哈希 / 会话 / 依赖）、`org.py`（成员 CRUD）、快照 schema 扩展、启动初始化与迁移回填。
2. **现有接口接鉴权**：全部业务路由挂 `require_org_member`，列表加 `org_id` 过滤，创建写归属。
3. **前端**：登录页 + 路由守卫、顶部组织标识 + 账号菜单、改密弹窗、成员管理页、`api.js` 401 处理。
4. **测试**：`tests/test_auth.py`（登录/改密/会话过期）、`tests/test_org.py`（新增/邀请/退出/可见性隔离），沿用现有 pytest + mock 约定。
5. **联调 & 迁移演练**：用现网 `app.db` 副本验证回填与兼容。

改动量预估：后端约 2 个新模块 + 现有路由批量加依赖；前端 1 个新页面组 + topbar 改造。建议拆成「后端鉴权」「前端登录与组织标识」「成员管理」三个 PR，各自 ≤ 400 行（对齐 CLAUDE.md §6.3）。
