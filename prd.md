# AI评估流程架构设计

## 一、项目背景与目标

**背景**：现有「智搜策略效果评估」全流程依赖人工：捞数、逐条打分、逐条写理由、手工汇总、手工写报告。

**改造目标**：将评估重构为 "人工定标准 + AI 做执行 + 人工兜置信" 的评测工作台，把三大能力沉淀为平台内置 Skill：

*   数据获取：自助拉数 + 清洗、支持
    
*   标注 Skill：分维度打分 + GSB 判定 
    
*   报告 Skill：汇总 + Badcase 聚类 + 报告生成；
    

## 二、功能列表

| 一级功能 | 二级功能 | 功能逻辑说明 |
| --- | --- | --- |
| 数据概览 | 核心指标卡 | 按时间范围（今日/近7天/近30天/自定义）展示本月评估量；支持按任务类型（通用评估、博文分析）区分统计； |
| 评估中心 | 任务管理 | 字段：任务ID、任务名称、状态、评估方式、关联评估基准、样本量、进度、创建时间、创建人、操作； |
|  | 顶部步骤条 | 顶部步骤条：数据准备 → AI评测 → 人工复核 → 报告； |
|  | 状态与操作逻辑 | *   已完成：查看报告 / 复制任务 / 删除任务<br>    <br>*   已停止：编辑任务 / 执行评测 / 复制任务 / 删除任务<br>    <br>*   未开始：编辑任务 / 执行评测 / 复制任务 / 删除任务<br>    <br>*   执行中：展示进度（如 1/200）／支持停止任务<br>    <br>*   执行失败（建议新增）：查看失败原因 / 重试 / 编辑任务 |
|  | 下载评估测试集 | 支持导出 Excel 表格，各维度得分、理由等 |
|  | 列表能力 | 搜索（任务名/ID）、筛选（状态/评估方式/策略版本/时间/裁判员模型）、 |
|  | 创建评测任务 | *   任务名称：必填<br>    <br>*   任务描述：非必填<br>    <br>*   裁判员模型：下拉选择模型<br>    <br>*   评测数据：选择已有数据集 ／`创建新数据集`按钮（跳转数据集创建页）<br>    <br>*   评估基准：选择评估指令<br>    <br>*   预估字数：按 数据条数 × 单条平均字数 × 维度数 自动计算，同步预估耗时与成本 |
| 数据集 | 数据集列表 | 字段：数据集名称、字数数量、数据条数、创建时间、创建人；<br>操作：删除、下载<br>限制判断：执行任务 |
|  | 创建数据集 | 方式1（上传）：数据集名称、数据集描述、上传数据（CSV/Excel/JSON/JSONL，≤50MB），提供标准模板下载、字段映射、格式校验（报错定位到行）<br>方式2（数据库直连）：链接数据库自动构建数据集——选择数据源(MCP)、配置时间范围/条数/采样策略/过滤条件 或 自定义SQL，AI自动字段映射后生成 （待开发） |
|  | 数据集例子 | 提供标准数据集样例（可预览/下载），说明对应评估标准一级必需列与格式规范，降低上传错误率 |
|  | 数据源连接管理 | 数据源列表、新增连接（地址/账号加密存储）、测试连接、连接状态；权限限定数据分析师配置 |
| 评估基准管理 | 基准列表 | 字段：基准名称、评估类型（提示词类型／技能Skill）​、评估方式（GSB／多维度）、使用次数、版本、更新时间、创建人；<br>操作：编辑、复制、删除、查看详情（被任务引用中不可删除） |
|  | 创建评估基准 | 基准名称、​基准描述、​评估类型：提示词类型 ／ Skill<br>提示词类型：提示词编辑器，支持模板变量（`{query}`／`{维度}`／`{评分标准}`等）、输出格式约束（JSON：分值 + 理由），建议增加置信度字段输出<br>Skill类型：支持上传技能评估包／选择已有Skill，展示技能描述 |
|  | 维度与规则配置 | 多维度：维度增删、权重设置（合计校验100%）、各分值评分标准描述<br>GSB：实验vs基线对象、Good/Same/Bad判定规则、裁决维度 |
| AI 评估模块 | 评测引擎 | AI 评估模块是平台的执行引擎层，承接「数据集 + 评估基准」输入，产出「评分结果 + 报告」 |
|  | Excel 读取器 | 读取Excel 内容，填入模型提示词/技能 槽位 |

## 三、产品流程

核心业务流程：

![image](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/a/0VxPvD7gu3kjOPVj/983a3947ff954e2c8bc34495273cc02b4087.png)

数据集构建：

![image](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/a/0VxPvD7gu3kjOPVj/85b20521ba22414f9e2754c09a88d7c34087.png)

评估基准构建：

![image](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/a/0VxPvD7gu3kjOPVj/7dab4bda17b4444ba42c451d836710f54087.png)

## 四、功能详细设计

### 评估中心

页面结构：

:::
标题「智搜策略效果评估」            \[＋ 创建评测任务\]

─────────────────────────────────────────────

💡 三步引导横幅（可关闭，不可点击）        ×

─────────────────────────────────────────────

任务管理                    🔍 搜索任务名/ID

\[状态▾\] \[评估方式▾\] \[时间▾\]            

─────────────────────────────────────────────

任务列表表格

─────────────────────────────────────────────

                              分页
:::

任务管理字段：

| **字段** | **说明** |
| --- | --- |
| 任务ID | 创建任务时，自动创建任务 |
| 任务名称 | 创建任务时，填写 |
| 状态 | 参考状态机 |
| 评估方式 | 多维度 / GSB（由基准自动带出） |
| 评估基准 | 基准名 |
| 样本量 | Query 条数 |
| 进度 | 仅执行中显示 `1/200`，其余显示 `—` |
| 创建时间 | 默认倒序 |
| 创建人 | 姓名 |
| 操作 | 主按钮 + `···` 菜单 |

创建：

| **字段** | **必填** | **控件与规则** |
| --- | --- | --- |
| 任务名称 | ✅ |  |
| 任务描述 | — | 文本域，右下角计数 `0/50` |
| 评估方式 | ✅ | 卡片单选：📊多维度评估 ／ ⚖GSB对比评估 |
| 评估基准 | ✅ | 下拉选择（只显示同方式的基准）+ 右侧只读预览维度与评分标准 |
| 评测数据 | ✅ | 下拉选已有数据集（显示 `N条/M字`）+ `⊕创建新数据集` |
| AI裁判员模型 | ✅ | 卡片单选，选择模型 |

### 数据集管理

开发流程：

数据源连接 ──┐

             ├─▶ 创建数据集 ─▶ AI清洗（可选） ─▶ 人工检查 ─▶ 数据集列表

本地文件 ────┘                                            │

                                                          ▼

数据集例子 ──指导格式──▶ 上传环节 ──▶           被评测任务引用（

页面结构

:::
数据集                              \[＋ 创建数据集\]

──────────────────────────────────────────────

🔍 搜索数据集名称    \[来源▾\] \[创建人▾\] \[时间▾\]

──────────────────────────────────────────────

数据集列表表格

──────────────────────────────────────────────

                                      分页
:::

字段设计

| **字段** | **说明** |
| --- | --- |
| 数据集名称 | 点击查看详情/预览 |
| 字数数量 | 全部样本文本字数总和，用于任务费用预估 |
| 数据条数 | 有效条数 |
| 创建时间 | 默认倒序 |
| 创建人 | 协 |
| 操作 | 删除、下载 |

**创建数据集**

页面结构

← 创建数据集

数据集名称 \*  \[                    \]

数据集描述    \[                0/200\]

数据来源 \*

┌────────────────┐

│📤 上传数据      │ │🔗 数据库直连    │

│ 本地文件导入    │ │ 自动构建 (待开发)

└────────────────┘ 

                          \[取消\] \[保存\]

| **项** | **规则** |
| --- | --- |
| 数据集名称 | 必填，≤50 字，同用户下不允许重名 |
| 数据集描述 | 非必填，≤200 字 |
| 上传数据 | 支持 CSV / Excel / JSON / JSONL；单文件 ≤50MB；拖拽或点选 |
| 标准模板下载 | 上传区旁提供模板下载入口，按评估方式分为「多维度模板」「GSB 模板」 |

方式 2：数据库直连（待开发，P1）

### 评估基准

页面结构

:::
评估基准管理                        \[＋ 创建评估基准\]

──────────────────────────────────────────────────

🔍 搜索基准名称  \[评估类型▾\]\[评估方式▾\]\[创建人▾\]  

──────────────────────────────────────────────────

基准列表表格

──────────────────────────────────────────────────

                                            分页
:::

字段设计

| **字段** | **说明** |
| --- | --- |
| 基准名称 | 点击进入详情； |
| 评估类型 | 标签：提示词类型 / 技能 Skill |
| 评估方式 | 标签：多维度 / GSB |
| 使用次数 | 被任务引用累计次数 |
| 更新时间 | 最近编辑时间，默认倒序 |
| 创建人 |  |
| 操作 | 编辑 / 复制 / 删除 / 查看详情 |

操作逻辑

| **操作** | **逻辑** |
| --- | --- |
| 查看详情 | 页面：评估类型与方式、提示词全文/skill结构 |
| 编辑 | 编辑提示词 |
| 复制 | 复制全部配置，名称 `原名称_副本`，版本重置为 `v1.0`，验证状态重置为未验证 |
| 删除 | 被任务引用中不可删除（见下表） |

**创建评估基准：**

Step1 基础信息 → Step2 指令配置 → 保存

**基础信息**

| **字段** | **必填** | **规则** |
| --- | --- | --- |
| 基准名称 | ✅ | ≤50 字，同评估类型下不允许重名 |
| 基准描述 | — | ≤200 字，说明适用场景 |
| 评估类型 | ✅ | 卡片单选：✍ 提示词类型 / 🧩 技能 Skill |
| 评估方式 | ✅ | 卡片单选：📊 多维度 / ⚖ GSB （评估类型属性） |

**提示词配置**

| **项** | **规则** |
| --- | --- |
| 编辑框 | 单个提示词编辑器，维度、权重、评分标准、判定规则全部由用户直接写在提示词内 |
| 可用变量 | `{query}`、`{待评内容}`、`{基线内容}`（GSB 用）；点击变量面板插入 |
| 标准输出格式 | 含 `分值 / 理由`  的 JSON 模板 |
| 辅助功能 | 语法高亮、全屏编辑、字数统计 |

**skill 配置**

| **项** | **规则** |
| --- | --- |
| 技能来源 | 上传技能包 |
| 解析展示 | 只读展示技能名称、版本、适用场景、技能描述 |
| 失败提示 | 明确指出原因（格式不支持 / 文件损坏） |