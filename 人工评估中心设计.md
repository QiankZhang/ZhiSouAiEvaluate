# 人工评估中心 · 产品设计

> 状态：**已实现**（2026-09-01）。后端 `site/backend/manual.py`、前端 `site/frontend/src/manual.jsx`。
> 关联：[prd.md](prd.md) · [技术方案.md](技术方案.md) · [组织与账号系统设计.md](组织与账号系统设计.md) · [API.md](API.md)
> 参考实现：`/Users/sunyingying/Desktop/multi-model-eval`（纯前端模型对比标注工具）

## 实现与设计的差异（以代码为准）

| 设计 | 实现 | 原因 |
| --- | --- | --- |
| `manual.py` 通过 `from . import main` 惰性取句柄 | main.py 在自身初始化完成后调用 `manual.init(...)` **注入** tasks/lock/next_id/persist/parse_rows/models/report_templates | 彻底断开 manual→main 的 import 环，便于单测注入替身 |
| 报告标签常量在 main.py，report.py 惰性 import main | `REVIEW_STATUS_LABELS` / `RESULT_REVIEW_STATUS_LABELS` 迁到 report.py，main.py 保留别名 | 去掉 report→main 的循环依赖隐患 |
| 汇总页始终重算 `summary` | 每次 `annotate` 都重算（含标注中的初步汇总） | 逻辑更简单，计算成本可忽略 |
| 开放问题逐一确认 | 全部按 §12 默认值实现 | 用户确认「按默认值开发」 |

单测：`site/backend/tests/test_manual.py`（11 例：三类上传解析、加权总分、覆盖总分、跳过、汇总、报告生成、导出、重名校验）。前端 `npm run build` 通过。

---

## 0. 一句话定位

「AI 评估中心」是 **AI 裁判员跑分 → 人工复核 → 报告**；
「人工评估中心」是 **人上传数据 → 人逐条标注 → 自动汇总 → （主动触发）AI 写报告**。

两者在导航栏平级，后端、路由、数据完全独立，互不影响。

---

## 1. 背景与目标

现有平台的打分链路强依赖裁判员大模型。有一类场景更适合纯人工：

- 样本量不大、但要求评委亲自读、亲自判（如竞品对比、改版验收）。
- 评测维度是临时定的，不值得为它建一套评估基准。
- 多轮对话类语料，AI 裁判难以稳定判读，需要人来通读整段会话。

目标：给评委一个**顺手的人工标注工作台**，覆盖三种标注形态，标完自动出汇总数据，并能一键让 AI 基于人工标注写一份文字报告。

**设计原则**（与全站一致）：后端纯标准库 FastAPI + SQLite 快照；前端 React + hash 路由，无新增构建依赖；不引入协作/分配/预标注等重型能力。

---

## 2. 需求确认（已与产品方对齐）

| # | 问题 | 结论 |
| --- | --- | --- |
| 1 | 三种标注类型 | **GSB 标注**、**多维度评估标注**、**多轮对话标注** |
| 2 | 「分维度」的维度清单从哪来 | **在创建标注任务时手填**（维度名 + 权重），不接入「评估基准」模块 |
| 3 | 后端如何组织 | **独立模块**，`MT-` 前缀，新增 `_manual_tasks` 集合与 `/api/manual-tasks` 路由，与 `_tasks` 完全并行 |
| 4 | 多轮对话的「左右」 | 按 **chat 气泡** 组织一整段会话：**左 = 用户输入（user）**，**右 = 模型输出（assistant）** |
| 5 | 标注完成后的「AI 生成报告」 | **复用「评估报告模板」模块**：创建任务时选一个报告模板，标注完成后由用户主动点按钮触发生成 |

### 2.1 待产品方拍板的开放问题（见 §12，已给默认值）

1. 会话标注是**整段一个分**，还是**逐轮打分**？——默认：整段一个分。
2. GSB 左右两栏哪侧是基线？——默认：**左 = 基线 `baseline`，右 = 实验 `content`**，任务设置里可对调。
3. 上传文件格式支持范围？——默认：GSB / 多维度用 `CSV`、`XLSX`；多轮对话用 `JSONL`（也兼容 `CSV`）。
4. 标注单元是否允许「跳过 / 无法判断」？——默认：允许，不计入汇总分母（GSB）或均分（多维度）。

---

## 3. 概念模型

```
人工评估中心
  └── 标注任务 ManualTask (MT-xxxx)
        ├── annotate_type: GSB | MULTI_DIM | CONVERSATION
        ├── 内联数据 data（上传时解析，随任务存储，不进「数据集」模块）
        ├── dimensions[]     ← 仅 MULTI_DIM / CONVERSATION，创建时手填
        ├── report_template_id  ← 引用「评估报告模板」模块的一条模板
        ├── units[]          ← 标注单元（每条样本 / 每个 session 一个）
        ├── summary          ← 标注满后自动汇总
        └── report           ← 用户主动触发后生成的 Markdown 报告
```

- **标注任务**归属「创建人当前所在组织」，可见性沿用全站中间件规则（组织成员可见组织全部数据），不新增 `org_id` 字段。
- **数据不进「数据集」模块**：数据集模块的 schema 绑死 `MULTI_DIM/GSB` 两种 `eval_method` 且没有会话结构；人工评估中心自己解析并内联存储，保持独立。
- **一个标注任务只有一种 `annotate_type`**，创建后不可改（与「评估基准的 eval_method 不可改」一致）。

### 3.1 数据结构（SQLite 快照内联字典，不建关系表）

```jsonc
// ManualTask
{
  "id": "MT-1001",
  "name": "9月改版-通用查询人工验收",
  "description": "",
  "annotate_type": "GSB",                  // GSB | MULTI_DIM | CONVERSATION
  "gsb_swap_sides": false,                 // 仅 GSB：true 时左右对调
  "dimensions": [                          // 仅 MULTI_DIM / CONVERSATION
    { "key": "relevance", "name": "相关性", "weight": 40 },
    { "key": "quality",   "name": "内容质量", "weight": 40 },
    { "key": "format",    "name": "呈现格式", "weight": 20 }
  ],
  "report_template_id": "RT-1000",
  "report_judge_model": "deepseek-v4-flash", // 生成报告用的模型，默认取一个 live 模型
  "status": "ANNOTATING",                  // ANNOTATING | COMPLETED
  "progress_done": 3,
  "progress_total": 20,
  "created_by": "张乾坤",
  "created_at": "2026-09-01 10:00",
  "units": [ /* 见下 */ ],
  "summary": null,                         // 见 §7
  "report": null                           // { status, markdown, generated_at } 见 §8
}
```

标注单元 `unit` 按类型三种形态：

```jsonc
// GSB
{ "index": 1, "query": "...", "content": "...", "baseline": "...",
  "judgment": "G" | "S" | "B" | null,   // null = 未标 / 跳过
  "skipped": false, "note": "", "annotated_by": "张乾坤", "annotated_at": "..." }

// MULTI_DIM
{ "index": 1, "query": "...", "content": "...",
  "dim_scores": { "relevance": 4, "quality": 3, "format": 5 },  // 1–5，缺项=未标完
  "total": 3.8,            // 自动加权，允许人工覆盖（overridden_total）
  "overridden_total": null,
  "skipped": false, "note": "", "annotated_by": "...", "annotated_at": "..." }

// CONVERSATION
{ "session_id": "s-001", "title": "订机票改签",
  "turns": [ { "turn": 1, "role": "user", "content": "..." },
             { "turn": 2, "role": "assistant", "content": "..." }, ... ],
  "dim_scores": { ... }, "total": 4.1, "overridden_total": null,
  "skipped": false, "note": "", "annotated_by": "...", "annotated_at": "..." }
```

### 3.2 上传文件格式

| 类型 | 必需列 / 字段 | 说明 |
| --- | --- | --- |
| **GSB** | `query, content, baseline` | 复用现有 `_parse_upload_rows` 的列名别名识别（`main.py` `COLUMN_ALIASES`） |
| **多维度** | `query, content` | 同上 |
| **多轮对话** | `session_id, turn, role, content`（可选 `title`） | 一行一轮；按 `session_id` 分组、`turn` 升序；`role ∈ {user, assistant}`。JSONL 每行 `{session_id,turn,role,content}`，或 CSV 同名列 |

- 会话数据也接受**嵌套 JSON**：`[{ "session_id": "...", "title": "...", "turns": [{ "role": "...", "content": "..." }] }]`。
- 解析失败逐行报错，定位到源文件行号，复用现有上传校验的错误结构 `{ line, message }`。
- 单文件 ≤ 50MB（沿用现有限制）。

---

## 4. 导航与路由

导航栏「人工评估中心」与「AI 评估中心」平级（已在 `main.jsx` `NAV` 落位，当前是空占位页）。

| hash 路由 | 页面 | 组件 |
| --- | --- | --- |
| `#/manual-eval` | 标注任务列表 | `ManualEvalPage` |
| `#/manual-eval/:id` | 标注工作台 | `ManualAnnotatePage` |
| `#/manual-eval/:id/summary` | 汇总 + AI 报告 | `ManualSummaryPage` |

新建前端文件 `site/frontend/src/manual.jsx`（与 `account.jsx` 一样独立成文件），在 `main.jsx` 里 import 并加 3 个路由分支。

---

## 5. 页面一：标注任务列表 `ManualEvalPage`

参考「AI 评估中心」列表（`TasksPage`）的骨架，但更轻。

- 顶部：标题「人工评估中心」+ 副标题「人工标注 · 逐条判读」+ 右侧「**+ 创建标注任务**」按钮。
- 一行引导条（`DismissibleBanner`）：`上传数据 → 逐条标注 → 自动汇总 → AI 报告`。
- 筛选：搜索（名称/ID）、标注类型（全部 / GSB / 多维度 / 多轮对话）、状态（标注中 / 已完成）、创建人。
- 列表列：任务 ID、名称、标注类型（`Badge`）、进度（`已标 N / 共 M` + `Progress`）、状态、创建人、创建时间、操作。
- 操作：
  - 标注中 → 「继续标注」（跳工作台）、删除
  - 已完成 → 「查看汇总」（跳汇总页）、「继续标注」（可回改）、删除
- 空状态：`EmptyState`「还没有标注任务，点右上角创建」。

### 5.1 创建标注任务 `CreateManualTaskModal`

分区表单（参考 `CreateDatasetModal` + `CreateReportTemplateModal`）：

1. **基本信息**：任务名称（必填，≤50）、描述（≤200）。
2. **标注类型**（必选，`option-cards` 三选一，创建后不可改）：
   - ⚖ GSB 标注 —— 字段 `query / content / baseline`
   - 📊 多维度评估标注 —— 字段 `query / content`
   - 💬 多轮对话标注 —— 字段 `session_id / turn / role / content`
3. **上传数据文件**（`Dropzone`）：按所选类型显示对应「必需列 / 下载模板 / 查看样例」，即时预览前若干条与解析结果统计（共 N 条 / M 个 session）。
4. **评分维度**（仅「多维度」「多轮对话」显示）：可增删的维度行，每行「维度名 + 权重(%)」，底部实时校验「权重合计必须 = 100」。提供「一键填入推荐维度」（相关性40 / 内容质量40 / 呈现格式20）。
5. **GSB 展示设置**（仅 GSB 显示）：单选「左基线 / 右实验」或「左实验 / 右基线」，对应 `gsb_swap_sides`。
6. **评估报告模板**（必选）：下拉选「评估报告模板」模块里的一条（`GET /api/report-templates`）。附「报告生成模型」下拉，默认取第一个 `live` 模型。
7. 底部：取消 / 创建。创建成功后直接进入工作台。

> 校验失败沿用后端 `422 { detail: { message, errors: [{line, message}] } }` 结构，`Modal` 内逐行红字展示。

---

## 6. 页面二：标注工作台 `ManualAnnotatePage`

统一布局（参考 multi-model-eval 的 `app-shell`：左列表 + 右主区）：

```
┌───────────────┬─────────────────────────────────────────────┐
│ 单元列表        │  当前单元展示区（按 annotate_type 切换形态）     │
│  · 搜索/筛选     │                                             │
│  · ①②③…（带标注 │  ┌───────────────────────────────────────┐  │
│    状态圆点）    │  │  内容展示（GSB 双栏 / 多维单栏 / 会话气泡）  │  │
│  · 未标 / 已标   │  └───────────────────────────────────────┘  │
│  · 上一条/下一条 │  ┌───────────────────────────────────────┐  │
│                │  │  标注区（G/S/B 或 维度打分）+ 备注 + 跳过   │  │
│                │  └───────────────────────────────────────┘  │
└───────────────┴─────────────────────────────────────────────┘
                          [ 完成标注 → 去汇总 ]（进度 100% 时高亮）
```

公共行为：

- 左侧列表每项显示序号 / query 摘要 / 标注状态圆点（灰=未标、绿=已标、黄=跳过）。
- 筛选：全部 / 未标注 / 已标注 / 已跳过。
- 键盘：`←/→` 切上一条下一条；数字键快捷打分（见各形态）。
- **即时保存**：每次打分 / 改备注 `PUT /api/manual-tasks/:id/annotate` 落库，无「提交」按钮（与 multi-model-eval 一致）。
- 首次给某条打分后，自动跳到下一条未标注项；回改已标注项不自动跳。
- 顶部进度条 `已标 N / 共 M`；100% 时「完成标注」按钮高亮，点击跳汇总页（后端此时把 `status` 置 `COMPLETED` 并算 `summary`）。
- Markdown 正文用现有 `<Markdown>` 组件（`markdown.jsx`）渲染。

### 6.1 GSB 标注形态 `GsbAnnotatePanel`

- 展示：**左右分栏**（`.gsb-split`），按 `gsb_swap_sides` 决定左右。栏头标「基线 / 实验」，正文 Markdown 渲染，各自独立滚动、等高。
- 顶部显示当前 `query`。
- 标注区：三个大按钮 **G（实验更好）· S（持平）· B（实验更差）**（措辞随 swap 调整为「左更好 / 持平 / 右更好」也可，默认按实验视角）+ 备注输入框（`note`，可选，失焦自动存）+ 「跳过 / 无法判断」。
- 快捷键：`1=G 2=S 3=B 0=跳过`。

### 6.2 多维度标注形态 `MultiDimAnnotatePanel`

- 展示：**单栏** Markdown（`content`），顶部 `query`。
- 标注区：每个维度一行 `.dim-score-row`：维度名 + 权重 + 1–5 分**分段按钮**（或滑杆）+ 该维度短评（可选）。
- 底部：**总分**，默认 = Σ(维度分 × 权重)/100 实时计算并显示；提供「手动覆盖总分」开关（`overridden_total`）。
- 备注框 + 「跳过」。
- 快捷键：聚焦某维度后按 `1–5` 打分。

### 6.3 多轮对话标注形态 `ConversationAnnotatePanel`

- 展示：**chat 气泡串**（`.chat-thread`），一个 `session` 一整段：
  - `role = user` → **左对齐**气泡（`.chat-msg.user`）
  - `role = assistant` → **右对齐**气泡（`.chat-msg.assistant`）
  - 气泡内 Markdown 渲染；按 `turn` 升序；长会话整体滚动。
  - 顶部显示 `title`（无则「会话 s-001」）与轮数。
- 标注区：与 §6.2 完全一致（维度打分 + 总分 + 备注 + 跳过），**针对整段会话给一个分**。
- 左侧列表项以 `session` 为单位（`已标 N / 共 M` 的 M = session 数）。

> 逐轮打分是 §12 待定项；若确定要做，`unit` 增加 `turn_scores[]`，标注区在每个 assistant 气泡下挂一个迷你打分条。

---

## 7. 页面三：汇总 + AI 报告 `ManualSummaryPage`

进入条件：`status = COMPLETED`（或标注中也允许查看「初步汇总」，标注变动后重算）。

### 7.1 自动分数汇总 `summary`

标注满自动生成，回改后重算。三种类型对应不同结构（**直接复刻 `report.build_report` 的 `summary` 形状**，以便报告生成复用）：

```jsonc
// GSB
"summary": {
  "eval_method": "GSB",
  "total": 20, "good": 11, "same": 4, "bad": 3, "skipped": 2,
  "win_rate": 55.0,               // good / (total - skipped)
  "net_win_rate": 44.4            // (good - bad) / (good + same + bad)
}

// MULTI_DIM / CONVERSATION
"summary": {
  "eval_method": "MULTI_DIM",     // 会话也记 MULTI_DIM，报告链路一致
  "total": 20, "scored": 18, "skipped": 2,
  "avg_total": 3.72,
  "dimensions": [
    { "key": "relevance", "name": "相关性", "weight": 40, "avg": 3.9, "low_count": 2, "low_ratio": 11.1 },
    ...
  ],
  "distribution": { "1": 0, "2": 3, "3": 6, "4": 7, "5": 2 },  // round(total)
  "weakest_dim": "呈现格式", "strongest_dim": "相关性"
}
```

页面展示：

- **GSB**：G/S/B 三色堆叠条 + 胜率 + 净胜率 + 跳过数；下方「被判 B 的样本」列表（query + 人工备注），可点开看双栏原文。
- **多维度 / 会话**：平均总分大数字 + 各维度均分横向条 + 总分分布小柱图 + 最弱/最强维度；下方「低分样本」列表（总分 < 3，query + 备注）。
- 通用：`StatCard` 行（样本量 / 已标 / 跳过 / 完成率）、导出按钮（§9 `export`）。

### 7.2 AI 生成报告（用户主动触发）

- 一个按钮「**AI 生成评估报告**」（`Button variant="primary"`）。未点过时下方是空态提示「点击按钮，由 AI 依据你的标注结果撰写报告」。
- 点击 → `POST /api/manual-tasks/:id/report` → 后端异步生成，`report.status` 走 `GENERATING → READY | FAILED`，前端轮询（1.5s，参考 `TaskDetailPage`）。
- 生成逻辑（**复用 `report.py`，最小适配**）：
  1. 把 `units` 整形成 `report.build_report` 期望的 `results` 结构（GSB 用 `scores.judgment`；多维/会话用 `scores.dimensions + total`；`reason` 字段填人工 `note`）。
  2. 构造一个「伪 benchmark」`{ eval_method, config: { dimensions } }` 和「伪 task」`{ name, id, task_type: "人工标注", judge_model: report_judge_model, benchmark_name: "人工标注", dataset_name: <文件名>, progress_total, created_at }`。
  3. `content = report.build_report(pseudo_task, results, pseudo_benchmark)`。
  4. `template = 取 report_template_id 对应模板`；`markdown = report.generate_report_markdown(pseudo_task, content, results, template)`。
     - `generate_report_markdown` 已内置「无 live 模型 / 失败 → 回退确定性五段式模板」，直接复用。
  5. 会话类型：`results` 里每条附 `content = 会话转写文本`（`user: … / assistant: …` 拼接，截断 ≤ 1500 字），让报告能引用具体对话片段。
- 报告展示：`<Markdown source={report.markdown} />` + 「下载 Markdown」（`GET .../report/markdown`）。
- 可重复生成（覆盖上一次）。生成消耗一次大模型调用额度，走现有网关额度校验（额度用尽时按钮禁用并提示）。

---

## 8. 后端设计 `site/backend/`

### 8.1 新增 `manual.py`（约 250–300 行，单文件）

仿 `accounts.py` 独立成模块，`main.py` 里 `app.include_router(manual.router)`。内部复用 `main.py` 已有的：

- `_parse_upload_rows` / `_remap_rows` / `COLUMN_ALIASES`（GSB、多维度上传解析）——需要把这几个函数提到可被 import 的位置，或在 `manual.py` 里 `from .main import _parse_upload_rows`（现有 `report.py` 已有 `from . import main` 的先例）。
- `accounts.creator_name()`、`now()`、`_next_id`（`MT` 前缀）。
- `report.build_report` / `report.generate_report_markdown`（报告）。
- `llm.is_live` / `llm.fetch_quota`（额度校验）。

### 8.2 持久化（`db.py`）

- `_DEFAULT_ID_SEQ` 增加 `"MT": 1000`。
- `load_state` / `save_state` 增加 `manual_tasks` 键（与 `tasks` 同规格的 JSON 快照）。
- `main._persist_state()` 把 `_manual_tasks` 一起传入 `db.save_state`。
- 会话数据可能较大——单任务上限建议 500 条样本 / 200 个 session，超限拒绝上传（避免快照膨胀）。

### 8.3 API 一览（前缀 `/api/manual-tasks`，全部需登录 + 在组织内，走现有中间件）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/manual-tasks` | 列表，支持 `annotate_type / status / created_by / q` 过滤 |
| `POST` | `/api/manual-tasks/upload` | multipart：`name, description, annotate_type, dimensions(JSON), gsb_swap_sides, report_template_id, report_judge_model, file`。解析入库，返回任务详情 |
| `GET` | `/api/manual-tasks/template?annotate_type=` | 下载对应类型的 CSV/JSONL 模板 |
| `GET` | `/api/manual-tasks/{id}` | 详情（含 `units`、`summary`、`report`） |
| `PUT` | `/api/manual-tasks/{id}` | 改名称/描述/维度名/报告模板（`ANNOTATING` 时；`annotate_type` 不可改） |
| `DELETE` | `/api/manual-tasks/{id}` | 删除 |
| `PUT` | `/api/manual-tasks/{id}/annotate` | body：`{ unit_key, judgment? , dim_scores?, overridden_total?, skipped?, note? }`。`unit_key` = `index`（GSB/多维）或 `session_id`（会话）。写单元 + 重算 `progress_done`；满则 `status=COMPLETED` 并算 `summary` |
| `GET` | `/api/manual-tasks/{id}/summary` | 返回 `summary`（未满时返回初步汇总） |
| `POST` | `/api/manual-tasks/{id}/report` | 触发 AI 报告异步生成；已在生成中返回 409 |
| `GET` | `/api/manual-tasks/{id}/report` | 报告状态 + 内容（`GENERATING / READY / FAILED`） |
| `GET` | `/api/manual-tasks/{id}/report/markdown` | 下载报告 Markdown |
| `GET` | `/api/manual-tasks/{id}/export` | 导出标注明细 CSV（见 §8.4） |

### 8.4 导出 CSV 列

- **GSB**：`index, query, judgment, note, annotated_by, annotated_at`（+ 可选 `content, baseline` 全文）
- **多维度**：`index, query, <各维度分…>, total, note, annotated_by, annotated_at`
- **会话**：`session_id, title, turns_count, <各维度分…>, total, note, annotated_by, annotated_at`

### 8.5 并发与锁

沿用 `main._lock`：`manual.py` 的写操作 import 并复用同一把应用级锁（报告生成的 LLM 调用放锁外，仅结果回写进锁，参考 `_run_evaluation`）。

---

## 9. 前端组件与样式

### 9.1 新增 `manual.jsx` 组件树

```
ManualEvalPage
  └── CreateManualTaskModal
ManualAnnotatePage
  ├── UnitList（左侧列表 + 筛选 + 进度）
  ├── GsbAnnotatePanel
  ├── MultiDimAnnotatePanel
  └── ConversationAnnotatePanel
        └── DimScoreEditor（多维/会话共用的维度打分组件）
ManualSummaryPage
  ├── ManualSummaryPanel（GSB 版 / 多维版）
  └── ManualReportView（<Markdown> + 下载）
```

复用现有组件：`Modal, Field, Button, Badge, Progress, Table, EmptyState, Dropzone, Menu, ConfirmDialog, StatCard, Markdown, toast, useLoad, downloadFile`。

### 9.2 `styles.css` 新增（约 80–120 行）

| 类 | 用途 |
| --- | --- |
| `.annotate-shell` | 左列表 + 右主区两栏布局（`grid-template-columns: 280px 1fr`），高度撑满内容区 |
| `.unit-list` / `.unit-item` / `.unit-dot`（`--muted/--success/--warning`） | 左侧单元列表与标注状态圆点 |
| `.gsb-split` | GSB 双栏 `grid-template-columns: 1fr 1fr; gap: 16px`，栏内 `overflow:auto` 等高 |
| `.gsb-col-head` | 栏头「基线 / 实验」标签 |
| `.chat-thread` | 会话容器，纵向 flex，`gap: 10px`，`overflow:auto` |
| `.chat-msg` + `.chat-msg.user`（左，`align-self:flex-start`）+ `.chat-msg.assistant`（右，`align-self:flex-end`，品牌色底） | chat 气泡 |
| `.dim-score-row` | 维度打分行：名称 + 权重 + 1–5 分段按钮 |
| `.score-seg` / `.score-seg.active` | 1–5 分段按钮 |
| `.annotate-actions` | 底部 G/S/B 大按钮行 |

窄屏（复用现有 `@media` 断点）：`.annotate-shell` 单列堆叠，`.gsb-split` 变上下。

---

## 10. 与 multi-model-eval 的对照（借鉴与差异）

| multi-model-eval 能力 | 本设计 |
| --- | --- |
| CSV/XLSX 上传、列名关键词识别 | ✅ 复用平台现有 `_parse_upload_rows` |
| 左右并排 Markdown、左好/相同/右好 | ✅ GSB 形态（G/S/B + 备注） |
| 质量评级（优秀/一般/差）+ 备注 | ➡️ 演化为「多维度打分 + 总分 + 备注」 |
| 划词局部反馈（好/坏 + 原因 + 高亮） | ❌ 本期不做，列入后续 |
| 即时保存、首评自动跳下一条、列表标记 | ✅ 沿用交互 |
| 总评分浮层（占比 / 净胜差异） | ✅ 演化为汇总页 |
| 标注版本存档 `saveVersionBtn` | ❌ 本期不做（任务本身即一份版本） |
| 「复制 AI 评测 Prompt」半自动 | ➡️ 升级为「一键 AI 生成报告」（平台已有网关，不需要复制到外部） |
| 导出 XLSX 给开发 | ✅ 导出标注明细 CSV（§8.4） |
| 移动端设备壳预览、网页截图服务 | ❌ 不相关，不做 |

---

## 11. 非目标（本期不做）

- 划词 / 片段级局部反馈与高亮。
- 多人标注分配、标注一致性（Kappa）计算、双盲。
- AI 预标注 / AI 辅助建议分。
- 会话逐轮打分（默认整段，见 §12）。
- 把人工标注数据回流到「数据集」模块或训练集。
- 标注版本存档 / 历史对比。

---

## 12. 开放问题（需产品方确认，否则按默认值实现）

| # | 问题 | 默认值 |
| --- | --- | --- |
| 1 | 会话标注：整段一个分 vs 逐轮打分 | 整段一个分 |
| 2 | GSB 左右默认哪侧是基线 | 左基线 / 右实验，任务可对调 |
| 3 | 上传格式范围（是否要 JSON/JSONL 全支持） | GSB/多维：CSV+XLSX；会话：JSONL+CSV |
| 4 | 「跳过 / 无法判断」是否需要 | 需要，不计入汇总分母 |
| 5 | 单任务样本量上限 | 500 条 / 200 session |
| 6 | 汇总页是否需要「低分/ B 样本」逐条下钻看原文 | 需要（只读双栏） |
| 7 | 报告生成模型是否让用户每次选，还是任务级固定 | 任务级固定，默认 **DeepSeek V4 Flash**（`deepseek-v4-flash`），创建时可改 |
| 8 | 标注中途能否修改评分维度（加/删维度） | 不能，维度创建后锁定（改动会使已标数据失效） |

---

## 13. 实施拆分建议（后续开发时）

1. **后端骨架**：`manual.py` + `db.py` 快照 + 上传解析 + `units` 生成 + `annotate` 写入 + `summary` 计算（不含报告）。配套 `tests/test_manual.py`（上传解析、三类汇总、跳过项、进度满触发 COMPLETED）。
2. **前端骨架**：`manual.jsx` 列表页 + 创建弹窗 + 三种标注形态工作台 + 即时保存。
3. **汇总页**：`ManualSummaryPage` + 导出 CSV。
4. **AI 报告**：`/report` 接口复用 `report.py` + 前端轮询展示 + 下载。
5. 联调、窄屏适配、`npm run build` 通过。

预估变更量：后端 ~1 新文件 + `db.py`/`main.py` 各数行；前端 ~1 新文件 + `main.jsx` 数行 + `styles.css` ~100 行。控制在单个 PR ≤ 400 行较难，建议按上述 1–2 / 3 / 4 拆 2–3 个 PR。
